#include <iostream>
#include <cmath>
using namespace std;

double r,v,m,k;
char ur,uv,um,uk; //Para las unidades usamos u( variable de la magnitud)
const double pi=(4*atan(1))

int main(void){
    
  cout <<"Posicion inicial: ";
  cin >> r >> ur;
  cout << r <<" "<< ur << endl
}
    if(ur!='m' || ur!='d' || ur!='c') {
         cout <<"Unidad de medida no valida para la posicion";
    return 0;}
    
   cout <<"Velocidad inicial: ";
   cin >> v >> uv;
    cout << v <<" "<< uv << endl;
    if(uv!='m' || uv!='d' || uv!='c') {
         cout <<"Unidad de medida no valida para la velocidad";
    return 0;}
cout <<"Masa: ";
cin >> m >>um;
cout << m <<" "<< um << endl;
    if(um!='k' || um!='K' || um!='g'  || um!='G' || m<0) {
         cout <<"Unidad de medida no valida para la masa o masa negativa";
    return 0;}
cout <<"Constante de recuperación: ";
cin >> k >> uk;
cout << k <<" "<< uk << endl;
    if(uk!='N' || uk!='d' || uk!='k'||  k<0) {
         cout <<"Unidad de medida no valida para la constante o K negativa";
    return 0;}

//if masa negativa:  decir que no puede ser
//if k negartiva lo mismo

//encadenar switch(unidades) para cada caso: 4 switch
    
    return 0;
}
